

// This file is being deprecated. All logic has been moved to src/firebase/index.ts
// and src/lib/firestore.ts to ensure a clear separation of concerns and
// prevent server-side execution of client-side code during builds.

// This file is intentionally left empty to avoid causing import errors in files
// that might still reference it, but it should be removed in a future cleanup.
export {};
